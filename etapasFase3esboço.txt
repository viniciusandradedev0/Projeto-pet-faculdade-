# 📋 Roadmap — FASE 2: BACK-END C# ASP.NET CORE
# Objetivo: substituir localStorage por banco real + autenticação JWT
# Projeto: backend/PawsPlace.Api  |  .NET 8  |  SQLite (dev)

# ============================================================
# FASE 2 — BACK-END
# ============================================================

- [x] Etapa 8 — Setup do projeto ASP.NET Core
  - Web API .NET 8 criada (dotnet new webapi)
  - Pacotes: EF Core Sqlite 8.0.15, BCrypt.Net-Next, JwtBearer 8.0.15
  - Models: Usuario, Animal, Pedido, Favorito
  - AppDbContext com índices únicos e relacionamentos
  - Program.cs: JWT + CORS + migrations automáticas
  - Migration inicial: CriarTabelasIniciais

- [x] Etapa 9 — Seed dos animais + verificação do banco
  - Data/Seed.cs criado com os 15 animais hardcoded
  - dotnet run confirmou: migration rodou + banco SQLite criado (56KB)

- [x] Etapa 10 — Autenticação JWT
  - JwtService: gera token HS256 com claims (NameIdentifier, Email, Name)
  - AuthService: cadastro (bcrypt work=12) + login com mensagem genérica
  - POST /api/auth/cadastro → 201 + TokenResponseDto
  - POST /api/auth/login    → 200 + TokenResponseDto
  - Erros: 409 email duplicado, 401 credenciais inválidas

- [x] Etapa 11 — Endpoints de animais (público)
  - GET /api/animais        → lista ordenada por espécie+nome
  - GET /api/animais/{slug} → 200 ou 404

- [x] Etapa 12 — Endpoints de pedidos (autenticado)
  - POST /api/pedidos       → 201, bloqueia pedido duplicado pendente (409)
  - GET  /api/pedidos/meus  → lista do usuário logado, mais recente primeiro

- [x] Etapa 12.5 — Favoritos (autenticado)
  - POST   /api/favoritos/{animalSlug} → 201 ou 409 se já existe
  - DELETE /api/favoritos/{animalSlug} → 204 ou 404
  - GET    /api/favoritos/meus         → lista com dados do animal

- [x] Etapa 13 — Endpoints de perfil (autenticado)
  - GET    /api/usuarios/me → 200 com PerfilResponseDto
  - PUT    /api/usuarios/me → valida nome+telefone, 200 ou 400
  - DELETE /api/usuarios/me → 204 (cascade remove pedidos + favoritos)

- [x] Etapa 14 — Migração do front-end para consumir a API
  # ✅  Teste manual validado — todos os 6 blocos passaram
  #     Fazer antes de considerar 100% validado.
  # Favoritos no front ficam para DEPOIS desta etapa
  #
  # DECISÕES DE IMPLEMENTAÇÃO:
  # 1. "Lembrar de mim": JWT em localStorage se marcado, sessionStorage se desmarcado
  #    → mantém comportamento atual. Remoção desta feature planejada para Fase 3.
  # 2. API URL: auto-detectada por config.js (localhost em dev, URL real em prod)
  # 3. animais.json: mantido como fallback se API cair. Remover após deploy confirmado.
  #
  # CRIAR:
  - scripts/config.js          — API_BASE auto-detectada por hostname
  #
  # REESCREVER:
  - scripts/auth.js            — fetch /api/auth/* + paws-jwt em localStorage ou sessionStorage
  - scripts/data.js            — fetch GET /api/animais (fallback: animais.json)
  - scripts/adotar.js          — fetch POST /api/pedidos
  - scripts/meus-pedidos.js    — fetch GET /api/pedidos/meus
  - scripts/perfil.js          — fetch GET/PUT/DELETE /api/usuarios/me
  - scripts/proteger-rota.js   — decodifica JWT, verifica expiração
  - scripts/storage.js         — simplificar: manter só tema, jwt e redirect
  #
  # MANTER SEM ALTERAÇÃO:
  - validacao.js, filtros.js, render.js, tema.js
  - animacoes.js, voltar-topo.js, header.js, bootstrap.js
  - modal.js, animais-page.js, main.js, cadastro.js
  - login.js, contato.js, sobre.js
  #
  # localStorage APÓS migração:
  # REMOVE: paws-usuarios, paws-sessao, paws-pedidos
  # ADICIONA: paws-jwt (token JWT, só se "lembrar de mim" marcado)
  # MANTÉM: paws-tema, paws-mensagem-redirect, paws-redirect-pos-login

- [x] Etapa 15 — Deploy do back-end no Railway ✅ CONCLUÍDO (10/05/2026)
  - Plataforma: Railway (deploy via GitHub, PostgreSQL gratuito incluso)
  - Program.cs: auto-detecta DATABASE_URL (PostgreSQL) ou SQLite
  - Program.cs: CORS_ORIGINS via env var ou appsettings
  - Npgsql.EntityFrameworkCore.PostgreSQL instalado
  - railway.toml: builder trocado de NIXPACKS → DOCKERFILE (Nixpacks defaultava para .NET 6)
  - Dockerfile multi-stage com SDK 8.0 oficial da Microsoft
  - Branch de deploy: desenvolvimento (não main)
  - URL de produção: https://projeto-pet-faculdade-production.up.railway.app
  - Deploy status: ACTIVE — "Deployment successful"
  - scripts/config.js já apontava para a URL correta (configurado na Etapa 14)
  # ✅  CONCLUÍDO: API em produção com PostgreSQL online no Railway
  # ⏳  PRÓXIMO: validar todos os endpoints com PawsPlace.Api.http no outro computador


# ============================================================
# FASE 3 — MELHORIAS FUTURAS (PÓS-BACKEND)
# ============================================================

- [x] Etapa 16 — Validação completa em produção ✅ (11/05/2026)
  # Todos os 13 endpoints validados via curl contra a URL de produção.
  # URL base: https://projeto-pet-faculdade-production.up.railway.app
  #
  # PROBLEMAS CORRIGIDOS DURANTE A VALIDAÇÃO:
  # 1. Migration SQLite-only quebrava INSERT em PG (Id sem IDENTITY)
  #    → Program.cs: Migrate() trocado por EnsureCreated()
  # 2. Npgsql não parseia URI (postgresql://...) — quebrava com "Host can't be null"
  #    → Program.cs: BuildNpgsqlFromUri() converte URI → keyword=value
  # 3. Referências Railway quebradas após recriar Postgres
  #    → Raw Editor: ${{Postgres-fd52b244-...}} → ${{Postgres.X}}
  # 4. Adicionado log diagnóstico [DB] no startup e fallback via PG* env vars
  #
  # RESULTADO (todos 200/201/204/401 conforme esperado):
  #   1. GET  /api/animais          → 200 (15 animais)
  #   2. GET  /api/animais/{slug}   → 200
  #   3. POST /api/auth/cadastro    → 201
  #   4. POST /api/auth/login       → 200 + JWT
  #   5. POST /api/auth/login (404) → 401
  #   6. POST /api/pedidos          → 200
  #   7. GET  /api/pedidos/meus     → 200
  #   8. POST /api/favoritos/{slug} → 201
  #   9. GET  /api/favoritos/meus   → 200
  #  10. DELETE /api/favoritos/{slug} → 204
  #  11. GET  /api/usuarios/me      → 200
  #  12. PUT  /api/usuarios/me      → 200
  #  13. DELETE /api/usuarios/me    → 204 (cascade delete confirmado)
  #
  # PENDENTE: testar fluxo end-to-end pelo front no GitHub Pages
  #           https://viniciusandradedev0.github.io/Projeto-pet-faculdade/

- [ ] Remover "Lembrar de mim" ou substituir por refresh token real
      (JWT com expiração curta + refresh token em httpOnly cookie)
- [ ] Remover data/animais.json após validação em produção confirmada
- [x] Favoritos no front (coração nos cards + favoritos.html + toggle via API)
- [x] Atualizar perfil (nome, telefone) — PUT /api/usuarios/me implementado em perfil.html
- [ ] Painel admin: gerenciar animais e atualizar status dos pedidos
- [ ] Upload de foto de perfil (Azure Blob ou Cloudinary)
- [ ] Notificações por e-mail ao mudar status do pedido (SendGrid)
- [ ] Paginação nos endpoints de animais e pedidos
- [ ] Testes de integração no back-end (xUnit + WebApplicationFactory)
- [ ] Migração para Blazor WebAssembly (front em C# consumindo a mesma API)
